<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-jar-plugin</artifactId>
            <version>3.1.0</version>
            <configuration>
                <archive>
                    <manifest>
                        <mainClass>com.mycompany.TestLogin.LoginCheck</mainClass> <!-- Substitua pelo caminho completo da sua classe -->
                    </manifest>
                </archive>
            </configuration>
        </plugin>
    </plugins>
</build>
